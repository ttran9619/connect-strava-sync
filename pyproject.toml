[project]
name = "connect-strava-sync"
version = "0.1.0"
description = "A CLI application to push Garmin Connect names and descriptions to Strava."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
